{
  "name": "orderpilot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "OrderPilot — full-stack e-commerce order management (React SPA + Express-on-Vercel-functions + SQLite)",
  "engines": { "node": "20.x" },
  "scripts": {
    "build": "npm --prefix client ci --no-audit --no-fund && npm --prefix client run build",
    "start": "npm --prefix server run dev"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
